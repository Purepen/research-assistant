'use client'

export function AnimatedBackground() {
  // The landing page is white — background is handled per-section
  // This component just provides the subtle dot pattern for the hero
  return null
}